import styled from 'styled-components';

const OuterDiv = styled.div`
  display: block;
`;

const MainDiv = styled.div`
  transform-origin: 155px bottom;

`;

const ContentDiv = styled.div`
  
`;

export default {OuterDiv, MainDiv, ContentDiv}